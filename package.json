{
  "name": "demowebshop-playwright",
  "version": "1.0.0",
  "description": "Demo Web Shop Automation using Playwright with POM",
  "scripts": {
    "test": "playwright test"
  },
  "dependencies": {
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}
